// código não executável
// nessa aula o professor fala sobre uma parte mais conceitual

// procedural -> não estruturada (go to) -> baseada no procedimento
processamento(valor1, valor2, valor3)

// OO -> dados e funções -> atributos e métodos
// abstração (simplificação)
// encapsulamento (mostrar somente o necessário) -> baixo acoplamento
// herança (prototype) -> princípio "é um"
// priorize a composição ("tem um") ao invés da herança ("é um")
// polimorfismo -> objeto genérico para objetos mais específicos
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {

    }
}

objeto.processamento() // foco passou a ser o objeto e não a função
